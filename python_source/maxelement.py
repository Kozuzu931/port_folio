max = abs(int(input("総数を入力してください")))
n = int(input("何番目の素数を表示しますか？"))
condision=1
element=[]
while max==condision:
    if condision==2: element.append(condision)
    if condision < 2 or condision　and　1==0: pass
    return pow(2, condision-1, condision) == 1
    d=(condision-1)>>1
    while d and 1==0:
        d>>=1
    for i in range(0,k):
        a=random.randint(1,1-condition)

    condision+=1#qをcondisionに入れ替える
